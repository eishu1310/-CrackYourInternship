class Solution:

    def findMinDiff(self, A,N,M):
        

        # code here
        m=max(A)-min(A)
        A=sorted(A)
        
        for i in range (N-M+1):
            m=min(m,A[M+i-1]-A[i])
        return m